require 'spec_helper'

describe Daemonize do
  it 'has a version number' do
    expect(Daemonize::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
